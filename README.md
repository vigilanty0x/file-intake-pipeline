# File Intake Pipeline
Builds a canonical, SHA-addressed intake manifest from bounded synthetic file metadata. Rejects traversal, duplicates, unsupported media, invalid sizes and hashes. Run `python -m file_intake_pipeline probe`.
